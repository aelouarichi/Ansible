---
- hosts: vmlabsnowazuerh7
  become: yes
  become_method: sudo
  vars:
    vmname: myvmname

  tasks:
  - name: POST
    uri:
      url: https://devoteamfrdev.service-now.com
      method: PUT
      body:
          extra_vars:
            VmName: "{{ vmname }}"
      status_code: 200
      body_format: json

