---
- hosts: 10.1.17.10
  become: yes
  become_method: sudo
  vars:
    vmname: myvmname

  tasks:
  - name: POST
    uri:
      url: https://devoteamfrdev.service-now.com
      method: POST
      body:
          extra_vars:
            VmName: "{{ vmname }}"
      status_code: 200
      body_format: json

